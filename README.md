# MY WORD - v3 (PWA / Offline)

기획서 화면(밝은 회색 배경 + 흰 카드 + 하단 메뉴 + 중앙 +버튼) 스타일로 맞춘 v3입니다.

## 실행
```bash
npm install
npm run dev
```

## 빌드(배포용)
```bash
npm run build
npm run preview
```

## 오프라인(PWA)
- 첫 접속(설치) 후에는 캐시된 범위 내에서 오프라인에서도 실행됩니다.
- 데이터는 localStorage에 저장됩니다. (브라우저 데이터 삭제 시 사라질 수 있어 백업 권장)
